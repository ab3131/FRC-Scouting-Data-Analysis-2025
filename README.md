Open the scoutingweb folder and run _npm start_ inside it. This should open something on localhost, let it open a tab in your browser. 
Open the main folder and run _flask run_
These should start the backend and the frontend

If it doesn't work, open the inspect menu and go to _Console_. You will see the errors here.
